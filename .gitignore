node_modules/
dist/
.next/
.env
.env.local
*.log
.DS_Store
coverage/
.turbo/
prisma/*.db
*.tsbuildinfo

# Vendored agent skills (installed via `npx skills add`). Regenerable from
# skills-lock.json, which IS committed so the set and versions are pinned.
.agents/
.claude/skills/
